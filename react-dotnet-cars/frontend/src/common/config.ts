// application configuration
const config = {
    network: {
        baseEndPointUrl: 'http://localhost:5000',
        useMockData: false,
        corsEnabled: true
    },
    apiEndpoints: {
        cars: '/api/cars'
    }
};

export default config;

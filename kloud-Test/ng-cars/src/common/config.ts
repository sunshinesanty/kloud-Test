const config = {
    network: {
        baseEndPointUrl: 'https://kloudcodingtest.azurewebsites.net',
        useMockData: true,
        corsEnabled: false
    },
    apiEndpoints: {
        cars: '/api/cars'
    }
};

export default config;
